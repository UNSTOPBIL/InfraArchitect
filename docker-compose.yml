version: '3.9'
services:
  logic-tester:
    image: alpine
    command: ["echo", "Agent Loop Fixed"]
  db-primary:
    image: postgres:16-alpine
    volumes: ["test_data:/var/lib/postgresql/data"]
volumes:
  test_data: